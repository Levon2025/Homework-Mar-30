

#include <stdio.h>

 int main(){
  
 const int size = 5;
 int arr[size];
 for(int i = 0; i < size;i++){
  scanf("%d",&arr[i]);
}
  int max = arr[0];
  int index;
 for(int i = 0;i < size ; i++){
  if(max < arr[i]){
   max = arr[i];
   index = i;
}
}
 printf("%d %d",max ,index);
 return 0;
}
