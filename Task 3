#include <stdio.h>


 int main(){

  const int size = 5;
  int arr[size];
  for(int i = 0; i < size;i++){
   scanf("%d",&arr[i]);
}
  int lowest = arr[0];
  for(int i = 0; i < size ; i ++){
   if(lowest > arr[i]){
    lowest = arr[i];
}
}

  
  int max = arr[0];
  for(int i = 0; i < size ; i++){
  if(max < arr[i]){
   max = arr[i];
  
}
}

  int res = lowest + max;
  printf("%d",res);
  return 0;
}
